# 2026-09-23

## 项目功能完整性分析 + 实际运行（dev-renovation @ 8616bb2e）

### 一、功能完整性结论：**默认路径完整可用，但 nofxos 三档候选源已死**

`provider/nofxos` 仍被 `kernel/engine.go` 活跃调用，且用的是**内置废弃 key**
`cm_568c67eae410d912c54c`（`DefaultAuthKey`，BaseURL `https://nofxos.ai`）。实测三个端点：

| 端点 | 方法 | 实测 |
|---|---|---|
| `/api/ai500/list` | `GetAI500List` / `GetTopRatedCoins` | **401** `invalid api key` |
| `/api/oi/top-ranking` | `GetOITopPositions` | **402** deprecated |
| `/api/coin/{sym}` | `GetCoinData` | **402** deprecated |

⇒ 策略里 `coin_source.source_type` 选 **`ai500` / `oi_top` / `oi_low` 这三档会直接失败**
（`getAI500Coins` 等无降级，`err != nil` 直接返回）。

**但默认配置不受影响**：`GetDefaultStrategyConfig` 的 `CoinSource.SourceType = "hyper_main"`、
`UseAI500=false`、`UseHyperMain=true` ⇒ 走 Hyperliquid 原生数据。**开箱可用**。
（源码注释亦写明：NofxOS 数据集不覆盖 Hyperliquid 的 XYZ 资产，故默认关闭。）

候选源全档位现状：
- ✅ `static` / `hyper_all` / `hyper_main` / `hyper_rank` — 不依赖 nofxos
- ✅ `mixed` — AI500 失败只记日志不中断（有降级）
- ❌ `ai500` / `oi_top` / `oi_low` — 端点已死，选了必失败

`provider/nofxos/ai500_cache.go` 的 TTL 缓存只在 `GetAI500ListCached` 里，
而 engine 走的是 `GetTopRatedCoins()`，**没走缓存版本** ⇒ 冷启动无兜底。

### 二、启动前置条件（缺一不可，否则 Fatalf）

`config.MustInit()` 要求 `JWT_SECRET` ≥32 字节且不等于历史默认值；
`crypto.NewCryptoService()` 要求 `DATA_ENCRYPTION_KEY` + `RSA_PRIVATE_KEY`（PEM，换行写成字面 `\n`）。
`.env` 已被 `.gitignore` 覆盖（`.gitignore:34`），可安全生成。数据库 SQLite 自动建 `data/data.db`。

### 三、实测运行（后端 `/tmp/nofx-backend`，前端 3000）

- 启动链路全绿：配置 → 加密 → SQLite → 0 trader → API server，**0 ERROR / 0 panic**
- `/api/health` → 200 `{"status":"ok"}`
- 公开端点：`/supported-models`(DeepSeek/OpenAI…) `/supported-exchanges` `/traders` `/competition` 全 200
- 受保护端点（注册取 token）：`/my-traders` `/models` `/exchanges` `/strategies` `/strategies/default-config`
  `/market-insights/providers` 全 200
- **`/api/market-insights` 实测 1.45s 返回 3 个块**：`directional_signal`(1126) +
  `hyperliquid_flow`(1590) + `hyperliquid_leverage`(1627)
- `/api/symbols` → 234 个交易对；`/api/klines?symbol=BTC` → 真实 K 线（BTC 86385）
- `directional_signal` 输出**如实声明**「Order flow is not among the components this cycle」
  （侧车未启）⇒ 失败可见性按设计生效，不是静默降级

### 四、环境坑：vite dev server 在本沙箱跑不起来（非项目缺陷）

`npm run dev` 会**崩溃退出**，根因是沙箱 `node-safe-delete-shim` 的批量删除保护：
vite 懒发现依赖后重新优化要删 `node_modules/.vite/deps_temp_*`（58 → 79 个文件 > 阈值 50）。
第一次 `rm -rf node_modules/.vite` 后能起来，但**访问页面触发二次优化时再崩**。

同样根因也导致 `vite build` 默认输出（`prepareOutDir` 清空 `web/dist`）失败。

**可行绕法**（已验证）：
```bash
./node_modules/.bin/vite build --outDir /tmp/nofx-web-build --emptyOutDir   # 1m53s, EXIT=0
node /tmp/nofx-web-serve.js     # 极简静态服务 + /api 代理到 8080，监听 3000
```
`/tmp/nofx-web-serve.js` 是我写的临时脚本（SPA fallback + 路径穿越防护），仅沙箱环境用。

### 五、待办/遗留

- `CORS_ALLOWED_ORIGINS` 未设 → 启动 WARN，生产部署必须设
- 若要真正启用 HyperData 侧车，需先 `deploy/hyperdata/sidecar.sh install` 再把策略
  `enable_hyperdata` 打开（默认关闭是有意的）
- 本次注册了测试用户 `smoketest@nofx.local`（仅本地 SQLite，未配任何交易所/模型 key，不会交易）

---

## 死代码清理（约 7,000 行 / 31 个文件 / 5 个依赖，同日晚）

用户先问"项目里有什么多余代码"，逐项核实后动手清理。**所有删除前都 rsync 备份到
`/tmp/nofx-cleanup-20260923/`（31 个文件，保留相对路径）**——本沙箱 `git` 被 SIGTERM，
没有任何版本控制兜底。

### 删掉的
- 前端整条死图表链（4,730 行）：`AdvancedChart` ← `ChartTabs` ← `TraderDashboardPage`
  （根无人 import，整链可删）、`EquityChart`、`ChartWithOrders(Simple)`、`TradingViewChart`、
  `PageNotFound`、`BrandHero/BrandStats`、`stores/`、`useCounterAnimation`、`ui/input.tsx`、
  `lib/text.ts`、`utils/indicators.ts`、`i18n/pairs/metrics.ts`（空占位对象）
- Go（1,913 行 + 文件内 188 行）：`safe/` 整包（零 import）、`kernel/prompt_builder.go`
  （已被 `engine_prompt.go` 取代，仅自身测试引用）、`trader/{position_snapshot,helpers}.go`、
  `provider/nofxos/{ai500,ai500_cache,ai500_cache_test,oi,util}.go`、
  `market/data_indicators.go` 的 7 个 `ExportCalculate*` 测试桥（测试同包，改成调小写函数即可）
- npm：`date-fns`、`@radix-ui/react-slot`、`class-variance-authority`、`lightweight-charts`、`zustand`
- i18n：ui-strings 62 个死 key + translations.ts 的 `advancedChart`/`chartWithOrders`/`traderDashboard`
  三段（中英各一份，-112 行）

### 补的（真 bug）
4 个界面文案取用了不存在的 key → 中文下显示原始 key 或英文兜底：
`auth.loggingIn`、`chart.loadingChartData`、`chart.leadPnL`、`exchangeCfg.lighterApiKeyImported`

### 关键判定方法（可复用）
- 孤儿文件**按 import 语句**查，不能按名字子串（`ChartWithOrders` 会被 `ChartWithOrdersSimple` 假命中）
- 死 i18n key 判定用「字面量在 ui-strings.ts 之外是否出现」，先排除 2 个动态前缀
  （`loginRequired.`、`comparisonChart.`）；244 定义 → 182，0 悬空
- 保留项：`provider/coinank`（疑似预留的下一个 provider）、`%_claw402_%` 老数据兼容模糊匹配、
  `mcp/` 大量未用导出（对外库 API）、`ai500/oi_top/oi_low` 配置项（名字保留、实现已本地化）

### 验证
`go build ./...` = 0、`go vet ./...` = 0（含测试编译）、
`go test ./market ./kernel ./trader ./store` 全 ok、`tsc --noEmit` = 0、
`vite build` = 0（2923 modules，顺带修好了残缺的 `web/dist`：之前缺 `assets/`，托管会白屏）

### 沙箱新坑（已写进 `nofx-go-react-verify` skill）
- `git` 全被 SIGTERM（`git --version` 都不行）→ 只能靠 /tmp 备份
- **Bash `grep` 随机返回空或被杀** → 一律用 Grep 工具；但 ripgrep 有子串陷阱（`SafeInt` 命中 `SafeInternalError`）
- `tsc` / `vite build` / `vitest` **必须后台跑**，前台会 SIGTERM 或 `write EPIPE`

## 项目现状体检（清理后，只读）

产出 `PROJECT_ANALYSIS_2026-09-23.zh-CN.md`（根目录）。

### 健康度基线（全部实测）
`go build ./...`=0、`go vet ./...`=0、`go test ./...` 全绿（含 api 4s / manager 3s / marketdata 各包 /
binance 38s / coinank 82s）、`tsc --noEmit`=0、`vite build` 通过、后端 8080 与前端 3000 均 200。

### 规模
Go 275 文件（约 195 生产 + 80 测试）/ 约 5.85 万行生产代码；前端 134 源文件 / 约 3.1 万行；
docs 82 份 md；交易所适配器 **10 家**；npm 运行时依赖 15 个；DB 16 张表。

### 两处 P0（"外壳存在、实现缺失"，本次核心发现）
1. **账户级回撤熔断是空壳**
   - `trader/auto_trader.go:148-149` `MaxDailyLoss`/`MaxDrawdown` 只有定义，**全仓无读取**
   - `trader/auto_trader.go:182` `stopUntil` 只被检查（`auto_trader_loop.go:43`）与输出
     （`auto_trader_decision.go:81`），**全仓无赋值** → 暂停分支恒不成立
   - `trader/auto_trader.go:178` `dailyPnL` 只被重置（`auto_trader_loop.go:56`）与输出
     （`auto_trader_decision.go:193`），**无累加** → 上报的 `daily_pnl` 恒为 0
   - 对比：网格**有**真实现（`auto_trader_grid.go:133-205`）；提示词层有软约束
     （`kernel/schema.go:271-277` MaxDailyLoss=-10%）；持仓级有硬保护（`auto_trader_risk.go:18-30`）
2. **遥测默认开启且上报交易明细**
   - `config/config.go:78` `ExperienceImprovement: true`（默认开）
   - `telemetry/experience.go:126-136` 上报 exchange/symbol/amount_usd/leverage/user_id/trader_id
     → `google-analytics.com/mp/collect`（`:15-16`）；调用点 `trader/auto_trader_decision.go:357`
   - 可用 `EXPERIENCE_IMPROVEMENT=false` 关闭；有隐私政策文档

### P1
- `web/src/lib/api/config.ts:29` 调 `GET /api/prompt-templates`，**后端无此路由** → 404
- `docs/architecture/STRATEGY_MODULE.md` 仍写已删除的 `GetTopRatedCoins`/`GetOITopPositions`，
  且引用不存在的 `provider/data_provider.go`
- `README.md:26` 写 "nine exchanges"，实际 10 家（表漏 Indodax）
- nofxos 仅剩 `GetCoinData`，端点实测 402；`enable_quant_data` 默认 false 故不触发

### 数据源启用矩阵（默认零配置真实产出 3 个）
恒 true 且免密钥：`directional_signal`、`hyperliquid_flow`、`hyperliquid_leverage`
需 key：`coinank_liquidation`（`coinank_api_key` 非空）
需 sidecar：`hyperdata_orderflow`/`hyperdata_positioning`（`enable_hyperdata`，默认 false）
总开关 `enable_market_insights`（新策略默认 true）；`market_insight_sources` 空=全选

### 方法论（可复用，已沉淀为 skill）
**用 Grep 交叉验证"配置字段是否真被读取"** 是找"承诺与实现不一致"最快的方法：
字段只在定义处命中 = 死配置；值只被写不被读 = 逻辑空壳。比通读代码快一个数量级。

### 沙箱新增注意
`eslint`（`npx eslint .`）在沙箱内启动即报 broker-ipc-client IPC 错误（LINT=2），**无法取 lint 数**，
需标注为环境限制而非代码问题。`npx tsc` 可正常跑。
